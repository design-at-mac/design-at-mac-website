const search=document.getElementById("project-search");const cards=[...document.querySelectorAll(".project-card")];const empty=document.getElementById("no-results");if(search){search.addEventListener("input",()=>{const q=search.value.trim().toLowerCase();let shown=0;cards.forEach(card=>{const haystack=(card.dataset.search+" "+card.innerText).toLowerCase();const match=!q||haystack.includes(q);card.hidden=!match;if(match)shown+=1});empty.hidden=shown!==0})}
